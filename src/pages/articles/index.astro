---
import { getCollection } from "astro:content";
import CollectionPage from "../../components/CollectionPage.astro";
import { published } from "../../lib/site";

const posts = published(await getCollection("posts")).filter((post) => post.data.collection === "article");
---
<CollectionPage collection="article" posts={posts} />
